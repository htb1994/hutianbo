{
  "name": "channel-sales-ops-deploy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "deploy:build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && npm --prefix backend ci --include=dev && npm --prefix backend run build && rm -rf backend/public && cp -R frontend/dist backend/public",
    "deploy:start": "cd backend && npm run start"
  }
}
